- title( @user.name )
- page_type('dashboard')

#user-show-page

  .band.gray.spaced.large-screen
    .container

      - @user.marks_with_places.includes(:place).find_each do |mark|
        %p= link_to mark.place.name + " " + mark.updated_at.strftime('%d %b, %Y'), mark.place

      / .two-thirds.column
      /   .in-caps-header= "World View"

      /   .world-view-map-wrap
      /     .empty-map-box.world-view
      /       / %worldview_map.world-view-canvas{ user_id: @user.id, current_user_id: current_user_id, type: 'worldview' }
      /     .tab-caption= @user.decorate.travel_stats_string

      / .one-third.column
      /   .in-caps-header= "Profile & Info"

      /   .profile-and-info-wrap
      /     .profile-photo-box