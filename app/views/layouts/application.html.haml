!!!
%html{ data: { ng_app: "ngApp"} }
  %head
    %meta{charset: "utf-8"}/
    %meta{content: "NOODP", name: "ROBOTS"}/
    %meta{content: "initial-scale=1", name: "viewport"}/
    %title= content_for?(:title) ? yield(:title) : "Planit - Save and Share the Places You Love"
    = render 'application/google_analytics'
    = csrf_meta_tags
    = stylesheet_link_tag :application, media: "all"
  %body{ class: body_class, ng_controller: 'ApplicationCtrl' }
    = content_for?(:header) ? yield_with_blank(:header) : render_header
    = render "application/flashes"
    = yield
    = content_for?(:footer) ? yield_with_blank(:footer) : render_footer
    = render "application/javascript"
    = render 'application/feedback'
