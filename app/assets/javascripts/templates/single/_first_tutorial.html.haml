.initial-tutorial{ ng_show: '!m.planManager.itemCount()>0 && !m.addingItem && m.isLoaded && ( !m.hasLength( m.plans ) || m.plan() )' }
  #header-spacing.except-landscape
  .band.spaced
    .place-name-tutorial{ ng_show: 'm.plan() && !m.plan().currentLocation()', ng_class: "{'blur': hasPlaceNearbyOptions()}" }
      .container.centered
        .column
          = image_tag 'tutorial_arrow.png', { ng_class: "{'pad-top-addbox': m.plan()}" }
      .container.centered
        .column{ ng_show: 'm.plan() && !m.plan().currentLocation()' }
          %strong.neon.large Awesome!  Starting Where?
          %p Set a Location so You can Find Your Places
          .soft-tutorial
            .tutorial-image= image_tag "tutorial_place_nearby.png"
            %strong.neon.large
              .tutorial-line Where does your trip or list start?
              .tutorial-line You can change your location at any time;
              .tutorial-line we'll use it to find the places you search for