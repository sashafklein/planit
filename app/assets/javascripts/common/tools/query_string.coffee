angular.module('Common').factory 'QueryString', () ->
  # $s._changePathTo = (slug) ->
  #   pathArray = $window.location.pathname.split('/')
  #   pathArray.pop()
  #   newPath = pathArray.join('/') + "/#{slug}"
  #   $window.history.pushState("object or string", "Title", newPath)

